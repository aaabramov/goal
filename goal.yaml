workspace:
  desc: Current terraform workspace
  cmd: terraform
  args:
    - workspace
    - show

start:
  cmd: terraform
  desc: startscho
  assert:
    name: Check if on dev workspace
    ref: workspace
    equals: dev1
  args:
    - Success
