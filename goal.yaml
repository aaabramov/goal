test:
  desc: Echo test data
  cmd: echo
  args:
    - -n
    - dev

test1:
  desc: Test 1
  cmd: echo
  assert:
    desc: Check if dev
    ref: test
    expect: dev
  args:
    - Passed.

test2:
  desc: Test 1
  cmd: echo
  assert:
    desc: Check if stage
    ref: test
    expect: stage
    fix: terraform workspace select stage
  args:
    - Should not pass!

# Test stdin
scala:
  desc: Run scala REPL
  cmd: scala

# Test env vars being passed
env:
  desc: Show env vars
  cmd: env
